---
description: "Find the current public DeGov Agent API contract and complete operation reference."
---

# Current API Contract

The public API uses `/v2` and exposes 11 governance operations. Start with the [complete API reference](index.md) or the authoritative [OpenAPI specification](https://agent-api.degov.ai/openapi.json).

The reference describes the current contract only. Use new identifiers and cursors returned by the API, and check the exact parameters when updating an integration.
